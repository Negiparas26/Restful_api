<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Posts</title>
    <link rel="stylesheet" href="/style.css"/> 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> 
</head>
<body>
    <button id="darkModeToggle" class="toggle-button"><i class="fas fa-moon"></i></button> 
    <h2>Edit Your Post</h2>
    <p>Post id: <%= post.id %></p>
    <p>Post username: <%= post.username %></p>
   
    <form  class="post"method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <textarea rows="12" cols="77"   name="content" id="content"><%= post.content %></textarea>
        <br><center><button class="btn">Submit</button></center>
    </form>
    
    <script>
        const toggleButton = document.getElementById('darkModeToggle');
        toggleButton.addEventListener('click', () => {
            document.body.classList.toggle('dark-mode');
            const isDarkMode = document.body.classList.contains('dark-mode');
            localStorage.setItem('dark-mode', isDarkMode);
        });

        // Check local storage for dark mode preference
        if (localStorage.getItem('dark-mode') === 'true') {
            document.body.classList.add('dark-mode');
        }
    </script>
</body>
</html>
